# 不玩了 2.0.0-beta

这是社区版新包名的首次 Beta，重点是安全迁移和发布基础设施，不包含账号、收费或联网能力。

## 主要变化

- 社区版包名调整为 `io.github.yezg9920kely.buwanle`，可与旧版并存；
- 提供 `com.focusgate.app` / `1.4.8` 迁移桥接变体；
- 本地历史由 SharedPreferences JSON 幂等迁移到 Room；
- 新增 AES-256-GCM + PBKDF2 口令加密、版本号和 SHA-256 校验的数据导入/导出；
- 首次启动增加隐私确认，无障碍授权前增加独立醒目说明；
- 清理旧的公开品牌文案，统一显示“不玩了”；
- Release 构建启用 R8 和资源压缩，正式签名仅从环境变量读取；
- CI 分别测试、Lint 和构建社区版与迁移桥接版。

## 迁移旧数据

1. 不要先卸载旧版；
2. 使用与旧版签名一致的 `legacyBridge 1.4.8` 覆盖安装；
3. 在“数据迁移”中导出 `.bwl` 文件并设置至少 8 位口令；
4. 安装社区版，选择同一个文件并输入口令；
5. 核对今日/7 天/30 天数据后，再决定是否卸载旧版。

迁移文件不包含登录令牌或过期活跃会话。请妥善保管文件与口令。
